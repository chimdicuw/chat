// App.tsx (phiên bản không cần Header component)
import React from "react";

export default function App() {
  return (
    <div style={{ padding: 20, fontFamily: "Inter, Arial, sans-serif" }}>
      <header style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        padding: "12px 20px",
        background: "linear-gradient(90deg,#0f172a,#1e293b)",
        color: "#fff",
        borderRadius: 6,
        marginBottom: 16
      }}>
        <div>
          <h1 style={{ margin: 0, fontSize: 18 }}>Bài giảng Mr.C</h1>
          <p style={{ margin: 0, fontSize: 12, opacity: 0.9 }}>Ứng dụng tạo slide tự động</p>
        </div>
        <div style={{ fontSize: 13, opacity: 0.9 }}>Phiên bản 1.0</div>
      </header>

      <main>
        {/* Phần còn lại của App */}
      </main>
    </div>
  );
}
// App.tsx (phiên bản không cần Header component)
import React from "react";

export default function App() {
  return (
    <div style={{ padding: 20, fontFamily: "Inter, Arial, sans-serif" }}>
      <header style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "space-between",
        padding: "12px 20px",
        background: "linear-gradient(90deg,#0f172a,#1e293b)",
        color: "#fff",
        borderRadius: 6,
        marginBottom: 16
      }}>
        <div>
          <h1 style={{ margin: 0, fontSize: 18 }}>Bài giảng Mr.C</h1>
          <p style={{ margin: 0, fontSize: 12, opacity: 0.9 }}>Ứng dụng tạo slide tự động</p>
        </div>
        <div style={{ fontSize: 13, opacity: 0.9 }}>Phiên bản 1.0</div>
      </header>

      <main>
        {/* Phần còn lại của App */}
      </main>
    </div>
  );
}
